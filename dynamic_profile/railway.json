{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && gunicorn project_api.wsgi:application",
        "restartPolicyType": "ON_FAILURE"
    }
}
